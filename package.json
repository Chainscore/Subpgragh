{
  "name": "chain-score",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ prajaktathomas/chain-score",
    "create-local": "graph create --node http://localhost:8020/ prajaktathomas/chain-score",
    "remove-local": "graph remove --node http://localhost:8020/ prajaktathomas/chain-score",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 prajaktathomas/chain-score"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
